const config = {    
    // General configs
    port: 3001,
    authenticationType: 'G5', // (G5, SENIORX)
    
    // Database configs
    database: {
        host: 'localhost',
        user: 'sapiens',
        password: 'sapiens',
        database: 'XE'
    },
}

export default config;